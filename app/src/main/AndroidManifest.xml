<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artGalleryViewer">

    <application
        android:name="com.example.artGalleryViewer.ArtGalleryViewerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/agv_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/agv_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtGalleryViewer">

        <activity android:name="com.example.artGalleryViewer.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
